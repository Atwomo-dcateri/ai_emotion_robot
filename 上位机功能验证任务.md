# 上位机功能验证任务

## 验证目标

对上位机（树莓派）各功能模块进行独立验证，确保视觉、语音、决策、融合各模块在接入 STM32 下位机之前功能正确。

## 上位机模块架构

```
主循环 main.py
  ├── VisionModule       视觉模块       (需摄像头)
  │     └── FaceAnalyzer 表情分析器     (纯逻辑)
  ├── AudioController    语音模块
  │     ├── STT          语音识别       (需麦克风)
  │     └── TTS          语音合成       (需喇叭)
  ├── FusionModule       融合模块       (依赖 Vision + Audio)
  ├── DecisionController 决策控制器     (无硬件依赖)
  │     ├── LLMEngine    LLM 决策       (需 API Key)
  │     └── RuleEngine   规则引擎       (纯逻辑)
  └── CommController     通信控制器     (可切模拟模式)
```

## 当前进度

- **阶段 A — 纯逻辑验证**: ✅ 全部通过（232/232）
- **阶段 B — 视觉模块**: ✅ 全部通过（25/25）
- **阶段 C — 语音模块**: ✅ 全部通过（20/20）
- **阶段 D — 集成验证**: ✅ 全部通过（16/16）

## 测试脚本清单

| 脚本 | 覆盖阶段 | 状态 |
|------|---------|:----:|
| `test_comm_protocol.py` | A6 协议层 | ✅ 已完成 |
| `test_comm_hardware.py` | 下位机通信 | ✅ 已完成 |
| `test_config.py` | A1 Config 配置完整性 | ✅ 已完成 |
| `test_rule_engine.py` | A2 RuleEngine 规则引擎 | ✅ 已完成 |
| `test_decision.py` | A3/A4 DecisionController + LLMEngine | ✅ 已完成 |
| `test_fusion.py` | A5 FusionModule 数据融合 | ✅ 已完成 |
| `test_main_loop.py` | A7 主循环调度逻辑 | ✅ 已完成 |
| `test_vision.py` | B 视觉模块 | ✅ 已完成 |
| `test_audio.py` | C 语音模块 | ✅ 已完成 |
| `test_integration.py` | D 集成验证 | ✅ 已完成 |

---

## 阶段 A：无硬件依赖 — 纯逻辑验证

> 不依赖摄像头、麦克风、STM32，仅靠代码逻辑即可验证。

### A1 Config 配置完整性

| 编号 | 测试项 | 预期 | 状态 |
|:----:|--------|------|:----:|
| A1.1 | 所有配置类属性存在 | Config 类每个字段都有默认值，无 AttributeError | ⬜ |
| A1.2 | 配置类型正确 | 数值型字段为 int/float，文本型为 str，布尔型为 bool | ⬜ |
| A1.3 | 路径类配置有效 | `AUDIO_VOSK_MODEL_PATH` 指向存在的目录 | ⬜ |
| A1.4 | from_env() 方法 | 环境变量 `DEEPSEEK_API_KEY` 正确覆盖配置 | ⬜ |

### A2 RuleEngine 规则引擎完整测试

| 编号 | 测试项 | 输入 | 预期 | 状态 |
|:----:|--------|------|------|:----:|
| A2.1 | 开心情绪响应 | `face_emotion={emotion_cn:'开心'}` | OLED='开心'，包含开心类回复语音 | ⬜ |
| A2.2 | 悲伤情绪响应 | `face_emotion={emotion_cn:'悲伤'}` | OLED='悲伤'，安抚类语音 | ⬜ |
| A2.3 | 愤怒情绪响应 | `face_emotion={emotion_cn:'愤怒'}` | OLED='平静' (降级平息)，安抚语音 | ⬜ |
| A2.4 | 恐惧情绪响应 | `face_emotion={emotion_cn:'恐惧'}` | OLED='惊讶'，安抚语音 | ⬜ |
| A2.5 | 惊讶情绪响应 | `face_emotion={emotion_cn:'惊讶'}` | OLED='惊讶'，回应语音 | ⬜ |
| A2.6 | 平静情绪响应 | `face_emotion={emotion_cn:'平静'}` | OLED='平静'，问候类语音 | ⬜ |
| A2.7 | 无人脸无语音 | `has_face=False, speech_text=None` | 返回 `type='none'` 的空动作 | ⬜ |
| A2.8 | 关键词 "你好" | `speech_text='你好'` | 回复 "你好呀，很高兴见到你" + 点头动作 | ⬜ |
| A2.9 | 关键词 "谢谢" | `speech_text='谢谢'` | 回复 "不客气" | ⬜ |
| A2.10 | 关键词 "拜拜" | `speech_text='拜拜'` | 回复 "再见" | ⬜ |
| A2.11 | 关键词优先于情绪 | 同时有 `speech_text='你好'` 和 `face_emotion` | 优先触发关键词回复 | ⬜ |
| A2.12 | 心率过高告警 | `heart_rate=120, heart_rate_valid=True` | 高心率告警语音 | ⬜ |
| A2.13 | 心率过低告警 | `heart_rate=50, heart_rate_valid=True` | 低心率告警语音 | ⬜ |
| A2.14 | 血氧过低告警 | `oxygen=90, oxygen_valid=True` | 低血氧告警语音 | ⬜ |
| A2.15 | 传感器错误告警 | `sensor_status='error'` | 传感器异常语音，优先级最高 | ⬜ |
| A2.16 | 健康告警优先于其他 | 同时有高心率和 `speech_text` | 先触发健康告警 | ⬜ |

### A3 DecisionController 降级逻辑

| 编号 | 测试项 | 条件 | 预期 | 状态 |
|:----:|--------|------|------|:----:|
| A3.1 | LLM 禁用时使用规则引擎 | `DECISION_USE_LLM=False` | decide() 返回规则引擎结果 | ⬜ |
| A3.2 | LLM 无 API Key 时降级 | `DECISION_LLM_API_KEY=''` | is_ready() 返回 False，自动降级 | ⬜ |
| A3.3 | LLM 超时降级 | 设置短 timeout，调用 API 超时 | 降级到规则引擎，不崩溃 | ⬜ |
| A3.4 | LLM 返回空降级 | LLM 返回空列表 | 降级到规则引擎 | ⬜ |
| A3.5 | is_ready() 始终为 True | 任意配置 | 返回 True（规则引擎兜底） | ⬜ |

### A4 LLMEngine 调用测试

| 编号 | 测试项 | 条件 | 预期 | 状态 |
|:----:|--------|------|------|:----:|
| A4.1 | 构建 prompt | 传入标准 user_state | prompt 包含情绪、语音、健康信息 | ⬜ |
| A4.2 | 调用 DeepSeek API | 有有效 API Key | 返回 JSON，含 oled_emotion/speech/action | ⬜ |
| A4.3 | 解析 JSON 响应 | LLM 返回合法 JSON | 正确提取 oled_emotion/speech/action | ⬜ |
| A4.4 | 解析 markdown 代码块 | LLM 返回 ```json ... ``` | 正确提取 JSON 内容 | ⬜ |
| A4.5 | 缺失字段处理 | LLM 返回缺少 `speech` 的 JSON | 返回 None，触发降级 | ⬜ |
| A4.6 | JSON 解析失败 | LLM 返回非 JSON 文本 | 返回 None，触发降级 | ⬜ |
| A4.7 | 结果转动作指令 | 标准 LLM 响应 | 生成 oled + speak + servo 动作列表 | ⬜ |

### A5 FusionModule 数据融合

| 编号 | 测试项 | 输入 | 预期 | 状态 |
|:----:|--------|------|------|:----:|
| A5.1 | 仅视觉输入 | Vision 返回情绪，Audio/Comm 无数据 | `has_face=True, speech_has_new=False` | ⬜ |
| A5.2 | 仅语音输入 | Audio 返回语音文本，Vision 无数据 | `has_face=False, speech_has_new=True` | ⬜ |
| A5.3 | 仅健康数据 | Comm 上报健康数据 | `is_finger_detected=True`，健康字段有效 | ⬜ |
| A5.4 | 多模态同时输入 | 视觉+语音+健康同时有数据 | 状态字典包含所有输入 | ⬜ |
| A5.5 | 语音消费机制 | 读取一次后再次读取 | 第二次 `speech_has_new=False` | ⬜ |
| A5.6 | 自动消费模式 | `FUSION_CONSUME_SPEECH=True` | get_user_state() 后自动标记已消费 | ⬜ |
| A5.7 | 健康数据过期 | 停止更新健康数据超过 3s | `is_health_data_fresh=False` | ⬜ |
| A5.8 | get_user_state 返回格式 | 标准调用 | 包含 timestamp/has_face/has_speech/face_emotion/speech_text/ heart_rate/oxygen/is_finger_detected 等字段 | ⬜ |

### A6 CommController 模拟模式（已覆盖）

> 协议层 62 项测试已在阶段 1 完成。CommController 的模拟通路在 `comm_controller.py` 编写时已涵盖。

| 编号 | 测试项 | 状态 |
|:----:|--------|:----:|
| A6.1 | 模拟模式启动/停止 | ✅ |
| A6.2 | 发送 OLED 表情/文本/清屏指令 | ✅ |
| A6.3 | 发送舵机指令 | ✅ |
| A6.4 | 健康数据回调接收 | ✅ |

### A7 主循环调度逻辑

| 编号 | 测试项 | 输入 | 预期 | 状态 |
|:----:|--------|------|------|:----:|
| A7.1 | 循环仅在有状态变化时决策 | 无 face/语音/手指 | 不调用 decision.decide() | ⬜ |
| A7.2 | 动作指令分发 — OLED | action type='oled' | 调用 comm.send_oled_emotion() | ⬜ |
| A7.3 | 动作指令分发 — speak | action type='speak' | 调用 audio.respond() | ⬜ |
| A7.4 | 动作指令分发 — servo | action type='servo' | 调用 comm.send_servo_move() | ⬜ |
| A7.5 | 动作指令分发 — oled_text | action type='oled_text' | 调用 comm.send_oled_text() | ⬜ |
| A7.6 | 信号处理 | 收到 SIGINT/SIGTERM | 触发 shutdown_flag，安全退出 | ⬜ |
| A7.7 | 模块启动失败容错 | 任一模块 start() 返回 False | 仅打警告日志，不中断主循环 | ⬜ |

---

## 阶段 B：需摄像头 — 视觉模块验证

| 编号 | 测试项 | 操作 | 预期结果 | 状态 |
|:----:|--------|------|----------|:----:|
| B1 | 摄像头打开 | `VisionModule.start()` | start() 返回 True | ⬜ |
| B2 | 人脸检测 — 单人脸 | 面对摄像头 | `get_emotion()` 返回非 None，`face_count=1` | ⬜ |
| B3 | 人脸检测 — 无人脸 | 离开摄像头视野 | `get_emotion()` 返回 None | ⬜ |
| B4 | 人脸检测 — 多人脸 | 两人同时入镜 | `face_count>=2` | ⬜ |
| B5 | 表情识别 — 开心 | 对摄像头笑 | 识别为 'happy'/'开心' | ⬜ |
| B6 | 表情识别 — 平静 | 正常表情 | 识别为 'neutral'/'平静' | ⬜ |
| B7 | 表情识别置信度 | 各种表情 | `confidence` 值在 0-100 范围内 | ⬜ |
| B8 | 连续检测稳定性 | 持续运行 30s | 不崩溃，FPS 稳定 | ⬜ |
| B9 | 摄像头关闭 | `VisionModule.stop()` | stop() 无异常，资源释放 | ⬜ |
| B10 | 重复 start/stop | 连续 start→stop→start | 正常切换，无残留线程 | ⬜ |

---

## 阶段 C：需麦克风/喇叭 — 语音模块验证

| 编号 | 测试项 | 操作 | 预期结果 | 状态 |
|:----:|--------|------|----------|:----:|
| C1 | STT 启动 | `AudioController.start()` | start() 返回 True | ⬜ |
| C2 | TTS 语音合成 | `audio.respond('你好')` | 喇叭正常发声 | ⬜ |
| C3 | TTS 异步播放 | `audio.respond()` 返回后立即检查 | `is_speaking()` 返回 True | ⬜ |
| C4 | 唤醒词检测 | 说"你好" | AudioController 进入 LISTENING 状态 | ⬜ |
| C5 | 唤醒词回调 | 注册 `on_wake_word` 回调 | 唤醒时触发回调 | ⬜ |
| C6 | 语音识别 | 唤醒后说"今天天气怎么样" | 识别出文本，进入 PROCESSING | ⬜ |
| C7 | 监听超时 | 唤醒后不说话等待 5s | 自动回到 IDLE 状态 | ⬜ |
| C8 | 倾听时延 | 说一句话到识别文本可用 | < 3 秒 | ⬜ |
| C9 | 状态机完整流转 | IDLE→唤醒→录音→处理→回复→IDLE | 状态顺序正确 | ⬜ |
| C10 | ask_question() 提问 | 调用 `ask_question('你叫什么？')` | 先语音提问，然后监听回答 | ⬜ |
| C11 | STT 停止 | `AudioController.stop()` | stop() 无异常 | ⬜ |
| C12 | 重复 start/stop | 连续 start→stop→start | 正常切换，无残留线程 | ⬜ |

---

## 阶段 D：集成验证

> 将多个模块串联运行，验证完整数据通路。

| 编号 | 测试项 | 操作 | 预期结果 | 状态 |
|:----:|--------|------|----------|:----:|
| D1 | 视觉 → 决策 → 输出 | 打开摄像头，面对镜头 | Vision 检测到人脸 → RuleEngine 决策 → Comm 输出 OLED 指令 | ⬜ |
| D2 | 语音 → 决策 → 输出 | 说"你好" | Audio 唤醒 → STT 识别 → RuleEngine 关键词匹配 → speak 回复 + servo 点头 | ⬜ |
| D3 | 健康模拟注入 → 告警 | 通过 Comm 模拟器注入高心率数据 | Fusion 获取 → 健康告警 → speak 提醒语音 | ⬜ |
| D4 | 全流程主循环 | 运行 main.py (模拟模式) | 三路输入融合 → 决策 → 模拟输出无异常 | ⬜ |
| D5 | 异常恢复 | 测试中拔掉摄像头 | 不打崩溃，视觉模块优雅降级 | ⬜ |

---

## 测试脚本清单

| 脚本 | 覆盖阶段 | 状态 |
|------|---------|:----:|
| `test_comm_protocol.py` | A6 协议层 | ✅ 已完成 |
| `test_comm_hardware.py` | 下位机通信 | ✅ 已完成 |
| `test_config.py` | A1 Config 配置完整性 | ✅ 待运行 |
| `test_rule_engine.py` | A2 RuleEngine 规则引擎 | ✅ 待运行 |
| `test_decision.py` | A3/A4 DecisionController + LLMEngine | ✅ 待运行 |
| `test_fusion.py` | A5 FusionModule 数据融合 | ✅ 待运行 |
| `test_main_loop.py` | A7 主循环调度逻辑 | ✅ 待运行 |
| — | B 视觉模块 | ⬜ |
| — | C 语音模块 | ⬜ |
| — | D 集成验证 | ⬜ |
