# 通信模块测试任务

## 测试目标

验证上位机（树莓派）与下位机（STM32）之间通过串口进行数据通信的正确性和稳定性。

## 测试架构

```
测试脚本
  └── CommController (整合层)  ← 待补全
        ├── SerialComm (真实串口)
        │     ├── protocol.py (帧编解码)
        │     └── UART (物理层)
        └── STM32Simulator (回环模拟)
              ├── protocol.py (帧编解码)
              └── 回环验证
```

## 前置条件

- [ ] 补全 `communication/comm_controller.py`（实现 `CommunicationInterface`）
- [ ] 安装依赖：`pip install pyserial`
- [ ] （可选）安装 socat：`sudo apt-get install socat`（用于虚拟串口回环测试）

---

## 阶段 1：协议层单元测试

> 纯逻辑测试，无需硬件和串口，验证帧编解码的正确性

| 编号 | 测试项 | 输入 | 预期输出 | 状态 |
|:----:|--------|------|----------|:----:|
| 1.1 | 打包-解包往返 | `pack_frame(TYPE_HEARTBEAT, b'\x01\x02')` | `unpack_frame()` 返回 `(0x01, b'\x01\x02')` | ✅ |
| 1.2 | CRC 校验 — 篡改数据 | 正常帧 + 篡改 payload 中任意字节 | `unpack_frame()` 返回 `None` | ✅ |
| 1.3 | CRC 校验 — 篡改帧尾 | 正常帧 + 篡改尾部字节 | `unpack_frame()` 返回 `None` | ✅ |
| 1.4 | OLED 表情指令打包 | `pack_oled_emotion('开心', 85.0)` | 帧类型 `TYPE_OLED`，数据含表情名和置信度 | ✅ |
| 1.5 | OLED 文本指令打包 | `pack_oled_text('你好', 10, 20)` | 帧类型 `TYPE_OLED`，子命令 `OLED_CMD_TEXT` | ✅ |
| 1.6 | OLED 清屏指令打包 | `pack_oled_clear()` | 子命令 `OLED_CMD_CLEAR` | ✅ |
| 1.7 | 舵机指令打包 | `pack_servo_move(0, 90, 5)` | 帧类型 `TYPE_SERVO`，角度/速度正确 | ✅ |
| 1.8 | 查询传感器指令打包 | `pack_query_sensor()` | 帧类型 `TYPE_QUERY_SENSOR`，数据为空 | ✅ |
| 1.9 | 健康数据解析 | `b'\x48\x01\x62\x01'`（HR=72, O2=98） | `{'heart_rate':72, 'heart_rate_valid':True, 'oxygen':98, 'oxygen_valid':True}` | ✅ |
| 1.10 | 传感器状态解析 | `b'\x01\x00'` | `{'status':'online', 'error_code':0}` | ✅ |
| 1.11 | 异常 — 帧头错误 | `b'\x00\x00...'` | `None` | ✅ |
| 1.12 | 异常 — 帧尾错误 | 正常帧 + 尾部替换 | `None` | ✅ |
| 1.13 | 异常 — 长度不匹配 | 声明长度与实际不符 | `None` | ✅ |
| 1.14 | 异常 — 数据超长 | 超过 `MAX_DATA_LEN`(250) | 抛出 `ValueError` | ✅ |

---

## 阶段 2：Simulator 回环测试

> 使用虚拟串口对或直接回环，验证数据通路完整性

### 子任务 2.1：基于 socat 的虚拟串口回环

```bash
socat -d -d PTY,link=/tmp/ttyV0,raw,echo=0 PTY,link=/tmp/ttyV1,raw,echo=0
```

| 编号 | 测试项 | 操作 | 预期结果 | 状态 |
|------|--------|------|----------|------|
| 2.1.1 | SerialComm 初始化 + 连接 | 分别连接 `/tmp/ttyV0` 和 `/tmp/ttyV1` | `is_connected()` 返回 `True` | ⬜ |
| 2.1.2 | 帧发送与接收 | A 发帧 → B 监听 | B 的帧回调收到完整帧 | ⬜ |
| 2.1.3 | 粘包处理 | 连续发送多帧 + 部分帧数据 | 每帧独立触发回调，无乱序 | ⬜ |
| 2.1.4 | 断线重连 | 关闭一端串口后重新打开 | 自动重连成功，`is_connected()` 恢复 `True` | ⬜ |

### 子任务 2.2：STM32Simulator 回环

| 编号 | 测试项 | 操作 | 预期结果 | 状态 |
|------|--------|------|----------|------|
| 2.2.1 | 模拟器接收 OLED 指令 | 发送 OLED 表情帧 | 模拟器日志打印表情名称 | ⬜ |
| 2.2.2 | 模拟器接收舵机指令 | 发送 SERVO 帧 | 模拟器打印舵机角度 | ⬜ |
| 2.2.3 | 模拟器自动上报健康数据 | 等待 1s | 模拟器回调发出心跳帧 | ⬜ |
| 2.2.4 | 查询传感器响应 | 发送 QUERY_SENSOR 帧 | 模拟器回复健康数据帧 | ⬜ |
| 2.2.5 | 全双工通信 | 同时双向收发 | 无数据丢失或死锁 | ⬜ |

### 子任务 2.3：CommController 整合测试（模拟模式）

| 编号 | 测试项 | 操作 | 预期结果 | 状态 |
|------|--------|------|----------|------|
| 2.3.1 | 模拟模式启动 | `COMM_SIMULATION_MODE=True`, `start()` | 启动成功，`is_connected()` 返回 `True` | ⬜ |
| 2.3.2 | 发送 OLED 表情 | `send_oled_emotion('开心', 85)` | 模拟器打印日志 | ⬜ |
| 2.3.3 | 发送 OLED 文本 | `send_oled_text('Hello', 0, 0)` | 模拟器打印日志 | ⬜ |
| 2.3.4 | 发送舵机指令 | `send_servo_move(0, 90, 5)` | 模拟器打印日志 | ⬜ |
| 2.3.5 | 健康数据回调接收 | 注册 `on_health_data` 回调 | 1s 内收到模拟健康数据 | ⬜ |

---

## 阶段 3：真实硬件测试

> 需要 Raspberry Pi + STM32 物理连接

### 连接检查

- [ ] STM32 UART 引脚与树莓派 GPIO 正确连接（TX ↔ RX, RX ↔ TX, GND ↔ GND）
- [ ] 树莓派串口已启用（`/boot/config.txt` 中 `enable_uart=1`）
- [ ] 设备路径确认（`ls -l /dev/ttyS0` 或 `/dev/ttyAMA0`）
- [ ] 波特率匹配：双方都设为 115200

| 编号 | 测试项 | 操作 | 预期结果 | 状态 |
|------|--------|------|----------|------|
| 3.1 | 串口打开 + 连接确认 | `COMM_SIMULATION_MODE=False`, `start()` | `is_connected()` 返回 `True` | ⬜ |
| 3.2 | OLED 表情显示 | `send_oled_emotion('开心', 85)` | STM32 OLED 显示开心表情 | ⬜ |
| 3.3 | OLED 文本显示 | `send_oled_text('Hello', 10, 10)` | OLED 对应位置显示文本 | ⬜ |
| 3.4 | OLED 清屏 | `send_oled_clear()` | OLED 清空 | ⬜ |
| 3.5 | 舵机控制 | `send_servo_move(0, 90, 5)` | 点头舵机转到 90° | ⬜ |
| 3.6 | 舵机控制 — 摇头 | `send_servo_move(1, 45, 3)` | 摇头舵机转到 45° | ⬜ |
| 3.7 | 健康数据上报 | STM32 定时上报 | 每 1s 收到 `on_health_data` 回调 | ⬜ |
| 3.8 | 心率告警触发 | 模拟心率 > 100 | 规则引擎输出高心率告警动作 | ⬜ |
| 3.9 | 血氧告警触发 | 模拟血氧 < 95% | 规则引擎输出低血氧告警动作 | ⬜ |
| 3.10 | 断线重连 | 物理断开串口线后重新连接 | 自动重连成功 | ⬜ |

---

## 阶段 4：端到端集成测试

> 验证完整数据通路：传感器 → Fusion → Decision → 动作输出

| 编号 | 测试项 | 操作 | 预期结果 | 状态 |
|------|--------|------|----------|------|
| 4.1 | 融合模块获取健康数据 | FusionModule.get_user_state() | `heart_rate`/`oxygen` 字段非空 | ⬜ |
| 4.2 | 健康数据过期处理 | 停发健康数据 3s+ | `is_health_data_fresh` 为 `False` | ⬜ |
| 4.3 | LLM 决策 + 通信输出 | 调用 DecisionController.decide() | 返回的动作指令含 OLED/SERVO 类型 | ⬜ |
| 4.4 | 规则引擎健康告警 + 通信 | `decide()` 在高心率时 | 返回含心率告警语音的动作列表 | ⬜ |
| 4.5 | 主循环全流程 | 运行 `main.py`，模拟用户交互 | 视觉+语音+健康融合后，STM32 正确响应 | ⬜ |

---

## 前置修复任务

- [ ] 创建 `communication/comm_controller.py`，实现 `CommunicationInterface`
  - 支持 `SIMULATION_MODE` 切换（SerialComm / STM32Simulator）
  - 实现 `send_oled_emotion()` 等核心方法
  - 实现 `on_health_data()` / `on_sensor_status()` 回调注册
  - 整合帧打包逻辑
- [ ] 在 `config.py` 中确认 `COMM_SIMULATION_MODE` 配置项（当前第 61 行已有）

---

## 当前进度

- **阶段 1**: ✅ 62/62 通过
- **阶段 2**: ⬜ 未开始
- **阶段 3**: ⬜ 未开始
- **阶段 4**: ⬜ 未开始
- **前置修复**: ⬜ 未开始
